// import { selectLargeProgressBarDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectLargeProgressBarDomain();

describe('selectLargeProgressBarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
